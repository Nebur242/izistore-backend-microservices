name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Test Affected Projects
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: root
          POSTGRES_PASSWORD: rootpassword
          POSTGRES_DB: izistore
        options: >-
          --health-cmd="pg_isready -U root" --health-interval=10s --health-timeout=5s --health-retries=5

      redis:
        image: redis:latest
        ports:
          - 6379:6379

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Determine affected projects
        id: affected
        run: |
          echo "::set-output name=affected_projects::$(npx nx affected --target=build --base=main~1 --head=HEAD)"

      - name: Build affected projects
        run: npx nx affected --target=build --base=main~1 --head=HEAD

      - name: Run tests for affected projects
        run: npx nx affected --target=test --base=main~1 --head=HEAD

      - name: Run linting for affected projects
        run: npx nx affected --target=lint --base=main~1 --head=HEAD
