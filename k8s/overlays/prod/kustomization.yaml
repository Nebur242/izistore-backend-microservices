apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: prod

configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - NODE_ENV=production
